# =============================================================================
# API Configuration
# =============================================================================
EXPO_PUBLIC_API_URL=http://localhost:8080

# =============================================================================
# EAS Configuration
# =============================================================================
EXPO_PUBLIC_EAS_PROJECT_ID=

# =============================================================================
# Google OAuth (모바일)
# =============================================================================
# 발급 방법:
#   1. Google Cloud Console 접속: https://console.cloud.google.com/
#   2. 프로젝트 선택 후 "APIs & Services" > "Credentials"
#   3. "Create Credentials" > "OAuth client ID"
#
#   [iOS 클라이언트]
#   4. Application type: "iOS" 선택
#   5. Bundle ID: com.aido.mobile (production), com.aido.mobile.dev (development)
#   6. 생성된 Client ID 복사
#
#   [Android 클라이언트]
#   7. Application type: "Android" 선택
#   8. Package name: com.aido.mobile
#   9. SHA-1 인증서 지문:
#      - 디버그: keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android
#      - 릴리즈: EAS 빌드 후 "eas credentials" 명령으로 확인
#   10. 생성된 Client ID 복사
#
#   [웹 클라이언트 - Expo AuthSession용]
#   11. Application type: "Web application" 선택
#   12. Authorized redirect URIs: https://auth.expo.io/@aido-team/aido
#   13. 생성된 Client ID 복사 (EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID)
#
# 주의: iOS/Android 네이티브 로그인 시에는 각각의 Client ID 사용
#       Expo Go 개발 시에는 Web Client ID 사용
# -----------------------------------------------------------------------------
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-google-ios-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=your-google-android-client-id.apps.googleusercontent.com

# =============================================================================
# Apple Sign In (모바일)
# =============================================================================
# iOS 네이티브:
#   - 별도 Client ID 불필요 (Bundle ID 자동 사용)
#   - Xcode에서 "Sign in with Apple" Capability 추가 필요
#   - Apple Developer에서 App ID에 Sign in with Apple 활성화
#
# Android/웹 (Apple JS SDK 사용 시):
#   1. Apple Developer 접속: https://developer.apple.com/account/
#   2. "Certificates, Identifiers & Profiles" > "Identifiers"
#   3. "Services IDs" 생성:
#      - Identifier: com.aido.auth.android
#   4. "Sign In with Apple" 설정:
#      - Domains: yourdomain.com
#      - Return URLs: https://api.yourdomain.com/auth/apple/callback
#
# 주의: iOS는 expo-apple-authentication 사용 (네이티브 UI)
#       Android는 웹 기반 로그인 필요 (서버 연동)
# -----------------------------------------------------------------------------
# Android용 Apple 로그인 시 필요 (optional)
# EXPO_PUBLIC_APPLE_SERVICE_ID=com.aido.auth.android

# =============================================================================
# Kakao OAuth (모바일)
# =============================================================================
# 발급 방법:
#   1. Kakao Developers 접속: https://developers.kakao.com/
#   2. "내 애플리케이션" > 앱 선택
#   3. "앱 키" 탭에서 "네이티브 앱 키" 확인 (EXPO_PUBLIC_KAKAO_NATIVE_APP_KEY)
#   4. "플랫폼" 탭에서 iOS/Android 플랫폼 등록:
#
#   [iOS 설정]
#   5. 번들 ID: com.aido.mobile
#
#   [Android 설정]
#   6. 패키지명: com.aido.mobile
#   7. 키 해시 등록:
#      - 디버그: keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore -storepass android | openssl sha1 -binary | openssl base64
#      - 릴리즈: EAS credentials에서 SHA-1 확인 후 Base64 변환
#
#   [카카오 로그인 설정]
#   8. "제품 설정" > "카카오 로그인" 활성화
#   9. "동의항목"에서 필요한 정보 설정
#
# 주의: 네이티브 앱 키 사용 (REST API 키 아님)
#       react-native-kakao-login 라이브러리 필요
# -----------------------------------------------------------------------------
EXPO_PUBLIC_KAKAO_NATIVE_APP_KEY=your-kakao-native-app-key

# =============================================================================
# Naver OAuth (모바일)
# =============================================================================
# 발급 방법:
#   1. Naver Developers 접속: https://developers.naver.com/
#   2. "Application" > 앱 선택
#   3. "개요" 탭에서 Client ID, Client Secret 확인
#   4. "API 설정" 탭:
#
#   [iOS 설정]
#   5. 환경 추가: iOS
#   6. URL Scheme: naverlogin (또는 커스텀)
#
#   [Android 설정]
#   7. 환경 추가: Android
#   8. 패키지명: com.aido.mobile
#   9. 다운로드 URL: https://play.google.com/store/apps/details?id=com.aido.mobile
#
# 주의: 네이버는 Client Secret도 앱에서 필요
#       @react-native-seoul/naver-login 라이브러리 필요
# -----------------------------------------------------------------------------
EXPO_PUBLIC_NAVER_CLIENT_ID=your-naver-client-id
EXPO_PUBLIC_NAVER_CLIENT_SECRET=your-naver-client-secret
# iOS URL Scheme (네이버 로그인 후 앱 복귀용)
EXPO_PUBLIC_NAVER_URL_SCHEME=naverlogin

# =============================================================================
# Feature Flags
# =============================================================================
EXPO_PUBLIC_ENABLE_OFFLINE_MODE=true
EXPO_PUBLIC_ENABLE_BIOMETRIC_LOCK=true
EXPO_PUBLIC_ENABLE_VOICE_INPUT=true
EXPO_PUBLIC_ENABLE_CALENDAR_SYNC=true

# =============================================================================
# Monitoring (optional)
# =============================================================================
# EXPO_PUBLIC_SENTRY_DSN=
