/**
 * 공용 DTO 재수출 — 공개 표면 유지용
 *
 * UserIdParamDto는 todo 모듈도 사용하는 범용 파라미터 DTO라 @/common/dtos로
 * 이동했습니다. follow 모듈의 기존 공개 표면(dtos 배럴)은 그대로 유지합니다.
 */
export { UserIdParamDto } from "@/common/dtos/user-id-param.dto";
