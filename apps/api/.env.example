# ===========================================
# Aido API - Environment Variables Example
# ===========================================
# 이 파일을 복사하여 .env.development 또는 .env.production 생성
#
# 개발 환경: cp .env.example .env.development
# 운영 환경: cp .env.example .env.production
# ===========================================

# -----------------------------------------
# 필수 설정 (모든 환경)
# -----------------------------------------

# 실행 환경: development | production | test
NODE_ENV=development

# 서버 포트
PORT=8080

# PostgreSQL 데이터베이스 연결 문자열
# 형식: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# 예시: postgresql://postgres:postgres@localhost:5432/aido_dev?schema=public
DATABASE_URL=

# JWT 액세스 토큰 시크릿 (최소 32자)
# 생성 방법: openssl rand -base64 48
JWT_SECRET=

# JWT 리프레시 토큰 시크릿 (최소 32자)
# 생성 방법: openssl rand -base64 48
JWT_REFRESH_SECRET=

# JWT 만료 시간 (선택, 기본값: 15m)
# 형식: 15m, 1h, 1d 등
JWT_EXPIRES_IN=15m

# JWT 리프레시 토큰 만료 시간 (선택, 기본값: 7d)
JWT_REFRESH_EXPIRES_IN=7d

# -----------------------------------------
# 보안 설정
# -----------------------------------------

# CORS 허용 도메인 (쉼표로 구분)
# 예시: http://localhost:3000,http://localhost:8081
CORS_ORIGINS=http://localhost:3000

# Rate Limiting - 시간 윈도우 (밀리초, 기본값: 60000)
THROTTLE_TTL=60000

# Rate Limiting - 요청 제한 수 (기본값: 100)
THROTTLE_LIMIT=100

# -----------------------------------------
# OAuth 설정
# -----------------------------------------
# 개발 환경: 선택사항 (설정 없이 서버 실행 가능)
# 운영 환경: 최소 1개 이상의 OAuth 제공자 필수

# Google OAuth
# 발급처: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple OAuth (Sign in with Apple)
# 발급처: https://developer.apple.com/account/resources/identifiers
APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
# APPLE_PRIVATE_KEY는 .p8 파일 내용 (줄바꿈 포함)
APPLE_PRIVATE_KEY=

# Kakao OAuth
# 발급처: https://developers.kakao.com/console/app
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=

# Naver OAuth
# 발급처: https://developers.naver.com/apps
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# -----------------------------------------
# 외부 서비스 (선택)
# -----------------------------------------

# Expo Push Notifications
# 발급처: https://expo.dev/accounts/[account]/settings/access-tokens
EXPO_ACCESS_TOKEN=

# RevenueCat (인앱 결제)
# 발급처: https://app.revenuecat.com/
REVENUECAT_API_KEY=

# Redis (세션/캐시)
# 형식: redis://USER:PASSWORD@HOST:PORT
REDIS_URL=

# Sentry (에러 모니터링)
# 발급처: https://sentry.io/settings/[org]/projects/[project]/keys/
SENTRY_DSN=
