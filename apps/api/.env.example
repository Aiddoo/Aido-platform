# =============================================================================
# Server Configuration
# =============================================================================
NODE_ENV=development
PORT=8080

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/aido?schema=public"

# =============================================================================
# JWT Configuration
# =============================================================================
# JWT_SECRET: 액세스 토큰 서명에 사용하는 비밀 키
#   - 32자 이상의 랜덤 문자열 권장
#   - 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters

# JWT_REFRESH_SECRET: 리프레시 토큰 서명에 사용하는 비밀 키
#   - JWT_SECRET과 다른 값 사용 권장
JWT_REFRESH_SECRET=your-refresh-token-secret-key-minimum-32-characters

# 토큰 만료 시간
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# CORS Configuration
# =============================================================================
# 여러 도메인은 쉼표로 구분
CORS_ORIGINS=http://localhost:3000,http://localhost:8081

# =============================================================================
# Rate Limiting Configuration
# =============================================================================
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

# =============================================================================
# Google OAuth 2.0
# =============================================================================
# 발급 방법:
#   1. Google Cloud Console 접속: https://console.cloud.google.com/
#   2. 프로젝트 생성 또는 선택
#   3. "APIs & Services" > "Credentials" 이동
#   4. "Create Credentials" > "OAuth client ID" 클릭
#   5. Application type: "Web application" 선택
#   6. Authorized redirect URIs 추가:
#      - 개발: http://localhost:8080/auth/google/callback
#      - 프로덕션: https://api.yourdomain.com/auth/google/callback
#   7. Client ID와 Client Secret 복사
#
# 주의: Google Cloud Console에서 "OAuth consent screen" 설정 필수
#       - User Type: External (또는 Internal for Workspace)
#       - 필요한 Scopes: email, profile, openid
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:8080/auth/google/callback

# =============================================================================
# Apple Sign In
# =============================================================================
# 발급 방법:
#   1. Apple Developer 접속: https://developer.apple.com/account/
#   2. "Certificates, Identifiers & Profiles" 이동
#   3. "Identifiers" > "+" 클릭 > "Services IDs" 선택
#      - Description: Aido Web Login
#      - Identifier: com.aido.auth (APPLE_SERVICE_ID로 사용)
#   4. 생성된 Service ID 클릭 > "Sign In with Apple" 체크
#      - Domains: yourdomain.com (개발시 localhost 불가, ngrok 등 사용)
#      - Return URLs: https://api.yourdomain.com/auth/apple/callback
#   5. "Keys" > "+" 클릭 > "Sign in with Apple" 체크
#      - Key Name: Aido Auth Key
#      - 다운로드된 .p8 파일의 내용이 APPLE_PRIVATE_KEY
#      - Key ID가 APPLE_KEY_ID
#   6. Team ID는 우측 상단 계정명 옆 또는 Membership 페이지에서 확인
#
# 주의: Apple은 localhost를 허용하지 않음
#       개발시 ngrok 또는 유사 서비스로 HTTPS 터널 필요
# -----------------------------------------------------------------------------
APPLE_CLIENT_ID=com.aido.mobile
APPLE_SERVICE_ID=com.aido.auth
APPLE_TEAM_ID=your-apple-team-id
APPLE_KEY_ID=your-apple-key-id
# APPLE_PRIVATE_KEY: .p8 파일 내용 (-----BEGIN PRIVATE KEY----- 포함)
# 여러 줄이므로 따옴표로 감싸고 \n으로 줄바꿈 처리
APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT\n-----END PRIVATE KEY-----"
APPLE_CALLBACK_URL=https://api.yourdomain.com/auth/apple/callback

# =============================================================================
# Kakao OAuth
# =============================================================================
# 발급 방법:
#   1. Kakao Developers 접속: https://developers.kakao.com/
#   2. "내 애플리케이션" > "애플리케이션 추가하기"
#   3. 앱 생성 후 "앱 키" 탭에서 REST API 키 확인 (KAKAO_CLIENT_ID)
#   4. "제품 설정" > "카카오 로그인" 활성화
#   5. "제품 설정" > "카카오 로그인" > "Redirect URI" 등록:
#      - 개발: http://localhost:8080/auth/kakao/callback
#      - 프로덕션: https://api.yourdomain.com/auth/kakao/callback
#   6. "보안" 탭에서 Client Secret 생성 (KAKAO_CLIENT_SECRET)
#      - 코드 생성 후 "활성화" 필수
#   7. "동의항목" 설정:
#      - 닉네임, 프로필 사진: 필수 동의
#      - 카카오계정(이메일): 선택 동의 (비즈앱 전환시 필수 가능)
#
# 주의: 이메일 필수 수집은 비즈앱 전환 필요
#       https://developers.kakao.com/docs/latest/ko/kakaologin/prerequisite#personal-info
# -----------------------------------------------------------------------------
KAKAO_CLIENT_ID=your-kakao-rest-api-key
KAKAO_CLIENT_SECRET=your-kakao-client-secret
KAKAO_CALLBACK_URL=http://localhost:8080/auth/kakao/callback

# =============================================================================
# Naver OAuth
# =============================================================================
# 발급 방법:
#   1. Naver Developers 접속: https://developers.naver.com/
#   2. "Application" > "애플리케이션 등록" 클릭
#   3. 애플리케이션 정보 입력:
#      - 애플리케이션 이름: Aido
#      - 사용 API: "네이버 로그인" 선택
#      - 로그인 오픈 API 서비스 환경: "WEB" 추가
#   4. 필수 정보 선택:
#      - 회원이름, 이메일, 프로필 사진 등 필요한 항목 체크
#   5. 서비스 URL 및 Callback URL 입력:
#      - 서비스 URL: http://localhost:8080 (개발) 또는 https://yourdomain.com
#      - Callback URL: http://localhost:8080/auth/naver/callback
#   6. 등록 완료 후 Client ID, Client Secret 확인
#
# 주의: 검수 요청 전까지 등록된 테스터만 로그인 가능
#       "멤버관리" 탭에서 테스터 추가 필요
# -----------------------------------------------------------------------------
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret
NAVER_CALLBACK_URL=http://localhost:8080/auth/naver/callback
